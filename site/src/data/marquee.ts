/**
 * Marquee quotes for the homepage experience band.
 *
 * MOCK DATA for v1: these are builder-written, anonymous sample lines, exactly
 * as flagged in the proposal ("mock data for now, clearly flagged in code").
 * They are NOT real testimonials and are never presented as analytics or as
 * attributed quotes. When the backend serves real anonymous experience lines,
 * this array is replaced by that feed. Register: warm, honest, no exclamation
 * marks, zero em-dashes.
 *
 * The first four are the copy-deck examples (01-home.md); the rest continue the
 * same voice so the band has enough length to drift.
 */
export const marqueeQuotes: readonly string[] = [
  'Three weeks after reading it, I realized I hadn’t thought about it in days.', // MOCK
  'The chapter on boredom was the one that did it.', // MOCK
  'I did not quit. It quit me.', // MOCK
  'I read it twice. The second time was for my brother.', // MOCK
  'I kept waiting for the part where it made me feel ashamed. It never came.', // MOCK
  'For the first time it did not feel like giving something up.', // MOCK
  'I understood the pull, and then the pull just wasn’t interesting anymore.', // MOCK
  'My evenings are mine again, and I barely noticed them come back.', // MOCK
]
